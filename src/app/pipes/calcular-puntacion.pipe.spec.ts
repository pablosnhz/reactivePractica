import { CalcularPuntacionPipe } from './calcular-puntacion.pipe';

describe('CalcularPuntacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularPuntacionPipe();
    expect(pipe).toBeTruthy();
  });
});
